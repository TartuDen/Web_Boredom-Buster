<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>I'm bored</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <div class="container">
        <h1>Let's find something for you to do!</h1>
    </div>
    <div class="container">
        <form action="/select" method="post">
            <!-- Drop-down list for activity -->
            <select name="activity">
                <option value="">select activity</option>
                <% for(let type of types){ %>
                    <option value="<%= type %>">
                        <%= type %>
                    </option>
                    <% } %>
            </select>

            <!-- Drop-down list for participants -->
            <select name="participants">
                <option value="">select participants</option>
                <% for(let i=1;i<ppl;i++){ %>
                    <option value="<%= i %>">
                        <%= i %>
                    </option>
                    <% } %>
            </select>

            <!-- Submit button -->
            <button class="btn btn-primary" type="submit">Go</button>
        </form>
    </div>
    <% if(!err){ %>
        <div class="container">
            <textarea name="message" rows="5" cols="50"><%= data.activity %></textarea>
        </div>

        <div class="container">
            <p>
                <% if (locals.data.type) { %>
                    activity - <%= data.type %>;
                        <% } else { %>
                            activity - random;
                            <% } %>

                                <% if (locals.data.participants) { %>
                                    participants - <%= data.participants %>
                                        <% } else { %>
                                            participants - random
                                            <% } %>

            </p>


            <% }else{ %>
                <div class="container">
                    <h2>Wrong combination!</h2>
                </div>

                <% } %>

        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
            crossorigin="anonymous"></script>
</body>

</html>